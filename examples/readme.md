# airgap-coin-lib examples

You can use all example files in this folder with the following command after checking the project out and installing dependencies:

`npx ts-node ./examples/serializer/serialize.ts`
