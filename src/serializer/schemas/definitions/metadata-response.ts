export interface MetadataResponse {
  id: string // Message ID used to match request/reaponse
  name: string // Name of the app (eg. AirGap Vault)
  version: string // Verson of the app (eg. 1.0.0)
}
